package br.com.apps.churrascow.useCase

class ParticipantUseCase {

/*    *//**
     * Create a new participant with the received data from view.
     *
     * This participant will integrate the event.
     *
     * @param id event's id, used as foreign key
     * @param participantDto received data from ui layer.
     *//*
    fun newParticipant(id: Long, participantDto: ParticipantView) {
        service.newParticipant(id, participantDto)
    }*/

}